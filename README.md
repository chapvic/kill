# Quick process terminate (windows)

Version: 1.0

This tool provides quick process termination.

### Usage

```
kill <exename> [exename] ...

The `exename` is a process filename without path, for example: `notepad.exe`.

```
